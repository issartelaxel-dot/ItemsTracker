services:
  - type: web
    name: itemstracker-frontend
    runtime: static
    buildCommand: npm ci && npm run build
    staticPublishPath: dist
    pullRequestPreviewsEnabled: false
    envVars:
      - key: NODE_VERSION
        value: 22.22.0
      - key: VITE_BASE_PATH
        value: /ItemsTracker/
